# نظام الإشعارات — خدمات المساح والمقاول

## ما تم تجهيزه داخل التطبيق

- Firebase Cloud Messaging (FCM) لتسجيل جهاز المستخدم واستقبال Push Notifications.
- طلب إذن الإشعارات على Android 13+.
- قناة Android باسم `إشعارات التطبيق`.
- عرض الإشعار حتى عندما يكون التطبيق مفتوحًا (Foreground) بواسطة Local Notifications.
- حفظ FCM token للمستخدم في:
  `users/{uid}/fcmTokens/{tokenKey}`
- شاشة داخل التطبيق:
  `notifications/{uid}`
  لعرض الإشعارات وقراءتها.
- عداد إشعارات غير مقروءة في أعلى الصفحة الرئيسية.

## التحكم في الإشعارات الآن

### إشعار عام لكل المستخدمين
يمكنك استخدام Firebase Console > Messaging / Notifications Composer لإرسال إشعار عام لمستخدمي التطبيق.

### إشعار خاص لمستخدم
يمكن تخزين سجل الإشعار داخل:

`notifications/{uid}/{notificationId}`

بصيغة مثل:

```json
{
  "title": "طلبك اتقبل ❤️",
  "body": "تمت الموافقة على طلبك.",
  "createdAt": 1760000000000,
  "read": false
}
```

هذا يظهر في مركز الإشعارات داخل التطبيق.

## إرسال Push مخصص من الموقع

إرسال Push مباشر من JavaScript في الموقع باستخدام Firebase Admin SDK غير آمن لأن مفاتيح الخادم لا يجب وضعها داخل المتصفح.

لذلك، إذا أردت لاحقًا زرًا في لوحة تحكم الموقع مثل:

`إرسال إشعار لكل المستخدمين`

أو:

`إرسال إشعار لمستخدم محدد`

فالخطوة الصحيحة هي إضافة Backend/Cloud Function يستخدم Firebase Admin SDK، وتكون لوحة الموقع مجرد واجهة له. لا نضع Service Account أو Private Key داخل التطبيق أو الموقع.
